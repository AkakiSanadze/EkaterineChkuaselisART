# =============================================================================
# EKATERINE CHKUASELI - ART PORTFOLIO WEBSITE
# Apache Server Configuration File (.htaccess)
# =============================================================================
# This file configures Apache server settings for optimal performance,
# security, and user experience.
# =============================================================================

# -----------------------------------------------------------------------------
# 1. DEFAULT CHARSET
# -----------------------------------------------------------------------------
# Set UTF-8 as the default character encoding for all text files
AddDefaultCharset UTF-8

# -----------------------------------------------------------------------------
# 2. GZIP COMPRESSION
# -----------------------------------------------------------------------------
# Enable gzip compression to reduce file sizes and improve loading times
# Compresses text-based files before sending them to the browser

<IfModule mod_deflate.c>
    # Enable compression
    SetOutputFilter DEFLATE
    
    # Don't compress images (already compressed) or PDFs
    SetEnvIfNoCase Request_URI \
        \.(?:gif|jpe?g|png|webp|pdf)$ no-gzip dont-vary
    
    # Don't compress already compressed files
    SetEnvIfNoCase Request_URI \
        \.(?:exe|t?gz|zip|bz2|sit|rar)$ no-gzip dont-vary
    
    # Make sure proxies don't deliver wrong content
    Header append Vary User-Agent env=!dont-vary
    
    # Compress HTML, CSS, JavaScript, Text, XML and fonts
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE application/x-font
    AddOutputFilterByType DEFLATE application/x-font-opentype
    AddOutputFilterByType DEFLATE application/x-font-otf
    AddOutputFilterByType DEFLATE application/x-font-truetype
    AddOutputFilterByType DEFLATE application/x-font-ttf
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE font/opentype
    AddOutputFilterByType DEFLATE font/otf
    AddOutputFilterByType DEFLATE font/ttf
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE image/x-icon
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/xml
</IfModule>

# Alternative compression method using mod_gzip (older Apache versions)
<IfModule mod_gzip.c>
    mod_gzip_on Yes
    mod_gzip_dechunk Yes
    mod_gzip_item_include file \.(html?|txt|css|js|php|pl)$
    mod_gzip_item_include mime ^text/.
    mod_gzip_item_include mime ^application/x-javascript.
    mod_gzip_item_exclude mime ^image/.
    mod_gzip_item_exclude rspheader ^Content-Encoding:.*gzip.*
</IfModule>

# -----------------------------------------------------------------------------
# 3. BROWSER CACHING (EXPIRES HEADERS)
# -----------------------------------------------------------------------------
# Set expiration dates for static resources to reduce server requests
# and improve page load times for returning visitors

<IfModule mod_expires.c>
    # Enable expires module
    ExpiresActive On
    
    # Default expiration: 1 month
    ExpiresDefault "access plus 1 month"
    
    # HTML documents - 1 day (content may change frequently)
    ExpiresByType text/html "access plus 1 day"
    ExpiresByType application/xhtml+xml "access plus 1 day"
    
    # CSS and JavaScript - 1 year (versioned with cache busting)
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType application/x-javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    
    # Images - 1 year (rarely change)
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Favicon - 1 year
    ExpiresByType image/vnd.microsoft.icon "access plus 1 year"
    
    # Fonts - 1 year
    ExpiresByType font/ttf "access plus 1 year"
    ExpiresByType font/otf "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    
    # Media files - 1 year
    ExpiresByType audio/ogg "access plus 1 year"
    ExpiresByType video/ogg "access plus 1 year"
    ExpiresByType video/mp4 "access plus 1 year"
    ExpiresByType video/webm "access plus 1 year"
    
    # JSON/XML feeds - 1 hour (may update frequently)
    ExpiresByType application/json "access plus 1 hour"
    ExpiresByType application/xml "access plus 1 hour"
    ExpiresByType text/xml "access plus 1 hour"
</IfModule>

# Cache-Control headers (alternative/additional to Expires)
<IfModule mod_headers.c>
    # Set Cache-Control for static assets
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|ico|svg|woff|woff2|ttf|otf)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # HTML files - shorter cache
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "public, max-age=86400"
    </FilesMatch>
</IfModule>

# -----------------------------------------------------------------------------
# 4. IMAGE OPTIMIZATION - WEBP SUPPORT
# -----------------------------------------------------------------------------
# Add WebP image format support for modern browsers
# WebP provides superior compression compared to JPEG/PNG

<IfModule mod_mime.c>
    # Add WebP MIME type
    AddType image/webp .webp
</IfModule>

# Serve WebP images when browser supports it (requires mod_rewrite)
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Check if browser accepts WebP
    RewriteCond %{HTTP_ACCEPT} image/webp
    
    # Check if WebP version exists
    RewriteCond %{DOCUMENT_ROOT}/$1.webp -f
    
    # Serve WebP instead of JPEG/PNG
    RewriteRule (.+)\.(jpe?g|png)$ $1.webp [T=image/webp,E=accept:1]
</IfModule>

<IfModule mod_headers.c>
    # Vary header for WebP support
    Header append Vary Accept env=REDIRECT_accept
</IfModule>

# -----------------------------------------------------------------------------
# 5. SECURITY HEADERS
# -----------------------------------------------------------------------------
# Add security headers to protect against common web vulnerabilities

<IfModule mod_headers.c>
    # X-Frame-Options: Prevent clickjacking attacks
    # DENY - Page cannot be displayed in a frame
    # SAMEORIGIN - Page can only be displayed in frames on same origin
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # X-Content-Type-Options: Prevent MIME type sniffing
    # Forces browser to use declared content-type
    Header always set X-Content-Type-Options "nosniff"
    
    # X-XSS-Protection: Enable XSS filter in browsers
    # 1; mode=block - Enable XSS protection and block page if attack detected
    Header always set X-XSS-Protection "1; mode=block"
    
    # Referrer-Policy: Control referrer information
    # strict-origin-when-cross-origin - Send full URL for same origin, 
    # origin only for cross-origin, no referrer for HTTPS->HTTP
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Permissions-Policy: Control browser features
    # Restricts access to sensitive device APIs
    Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# -----------------------------------------------------------------------------
# 6. URL REWRITING
# -----------------------------------------------------------------------------
# Clean URLs and enforce best practices

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # -------------------------------------------------------------------------
    # 6.1 Force HTTPS (Optional - Uncomment if SSL is enabled)
    # -------------------------------------------------------------------------
    # RewriteCond %{HTTPS} off
    # RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
    
    # -------------------------------------------------------------------------
    # 6.2 Redirect www to non-www (Optional - Choose one based on preference)
    # -------------------------------------------------------------------------
    # Uncomment to redirect www to non-www:
    # RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    # RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
    
    # Uncomment to redirect non-www to www:
    # RewriteCond %{HTTP_HOST} !^www\. [NC]
    # RewriteRule ^(.*)$ https://www.%{HTTP_HOST}/$1 [R=301,L]
    
    # -------------------------------------------------------------------------
    # 6.3 Remove .html extension (Optional - for cleaner URLs)
    # -------------------------------------------------------------------------
    # Redirect external .html requests to extensionless URL
    # RewriteCond %{THE_REQUEST} \s/([^.]+)\.html [NC]
    # RewriteRule ^ /%1 [R=301,L]
    
    # Rewrite extensionless URLs to actual .html files
    # RewriteCond %{REQUEST_FILENAME} !-d
    # RewriteCond %{REQUEST_FILENAME} !-f
    # RewriteRule ^([^/]+)/?$ $1.html [L]
    
    # -------------------------------------------------------------------------
    # 6.4 Remove trailing slashes (Optional - for consistency)
    # -------------------------------------------------------------------------
    # RewriteCond %{REQUEST_FILENAME} !-d
    # RewriteRule ^(.*)/$ /$1 [R=301,L]
</IfModule>

# -----------------------------------------------------------------------------
# 7. ERROR PAGES
# -----------------------------------------------------------------------------
# Custom error page configurations

# 404 Not Found - Page does not exist
ErrorDocument 404 /404.html

# Other common error pages (uncomment if you create these pages)
# ErrorDocument 403 /403.html
# ErrorDocument 500 /500.html
# ErrorDocument 503 /503.html

# -----------------------------------------------------------------------------
# 8. ADDITIONAL OPTIMIZATIONS
# -----------------------------------------------------------------------------

# Enable Keep-Alive for persistent connections
<IfModule mod_headers.c>
    Header set Connection keep-alive
</IfModule>

# Disable ETags (use Last-Modified instead for better caching)
<IfModule mod_headers.c>
    Header unset ETag
</IfModule>
FileETag None

# Disable server signature (security through obscurity)
ServerSignature Off

# Limit request body size (prevent abuse)
# 10MB limit prevents large file uploads that could exhaust server resources
LimitRequestBody 10485760

# -----------------------------------------------------------------------------
# 9. DIRECTORY OPTIONS
# -----------------------------------------------------------------------------

# Disable directory browsing (security)
# Prevents users from viewing directory contents when no index file exists
Options -Indexes

# Follow symbolic links (if needed)
# Allows Apache to follow symbolic links in the directory
Options +FollowSymLinks

# -----------------------------------------------------------------------------
# 10. MIME TYPE CONFIGURATION
# -----------------------------------------------------------------------------
# Ensure proper MIME types for modern web formats

<IfModule mod_mime.c>
    # Ensure JavaScript files are served with correct MIME type
    AddType application/javascript .js
    
    # Ensure JSON files are served correctly
    AddType application/json .json
    
    # Ensure SVG files are served correctly
    AddType image/svg+xml .svg
    
    # Ensure WOFF2 fonts are served correctly
    AddType font/woff2 .woff2
    
    # Ensure WOFF fonts are served correctly
    AddType font/woff .woff
</IfModule>

# -----------------------------------------------------------------------------
# 11. CHARACTER ENCODING
# -----------------------------------------------------------------------------
# Ensure proper character encoding for all text files

# Add charset to various file types
AddCharset UTF-8 .html .css .js .json .xml .svg

# -----------------------------------------------------------------------------
# END OF .HTACCESS CONFIGURATION
# =============================================================================
# 
# NOTES FOR MAINTENANCE:
# 
# 1. To enable HTTPS redirection, uncomment section 6.1
# 2. To enable www/non-www redirection, uncomment section 6.2
# 3. To enable clean URLs (without .html), uncomment section 6.3
# 4. After making changes, test with: apachectl configtest
# 5. Restart Apache after changes: sudo systemctl restart apache2
#
# For support, see DEPLOYMENT.md in the project root
#
